# 更新日志

本仓库发布两个互不依赖的插件，各自独立编号：`v*` 标签发布 FmKit，`shop-v*` 标签发布 FmShop（见 `.github/workflows/build.yml`）。

## FmShop 1.0.0 —— 未发布（待打 `shop-v1.0.0`）

首个版本：系统商店与内置经济，不依赖 Vault 或任何前置插件。

- 商品表 1502 件、15 个分类：1374 条手写行（`prices.yml` 的 `items:`）+ 128 条由 `enchanted-books:` 曲线展开的附魔书行。
- 54 格 GUI：分类总览（三排 21 格图标位，现用 15 格）→ 分类页（每页 45 件）→ 确认页（27 格）；左键买、右键卖、Shift+右键卖出全部持有。
- 附魔书按曲线定价：`sell = base-sell × 等级^1.7 × 稀有度 × 诅咒系数`，稀有度默认取原版 `Enchantment#getAnvilCost()`；买入为卖出的 6 倍（素行为 2 倍）。
- 附魔书终身限售：每名玩家每种书终身只能卖 1 本，卖出后永久解锁购买；只有与商品行精确匹配的书可回收，多带一条附魔的书不回收。
- 内置经济：5% 卖出手续费、0% 买入手续费、2% 转账税，手续费默认销毁；`fmshop.fee.exempt` 可豁免。
- 每日限额（单笔上限 2304，凌晨 4 点重置）、动态行情（`market.yml`，按商品行 id 计价）、审计账本（保留 14 天，单笔超 10000 告警）。
- 管理指令 `/fsa`：give / take / set / price / market / resetlimit / resetunlock / reload / status / audit / doctor / tax。
- e2e：`bot/fmshop_test.js` 在真实 26.2 服务端 72/72 断言通过。

## FmKit 1.1.2 —— 2026-08-27

- 标题件数跟随刷新，翻页越界修正。

## FmKit 1.1.1 —— 2026-08-26

- 排序追加条目 id 次序键：同时间戳条目位置稳定，满包点击不再引起跳动。

## FmKit 1.1.0 —— 2026-08-26

- GUI 差量渲染，默认刷新间隔 1 秒。
- 存储改为单线程顺序写入。
- 箱日志四键配置。
- 构建改用 JDK 25（paper-api 26.2 的字节码为 Java 25），产物仍以 `--release 21` 编译。
